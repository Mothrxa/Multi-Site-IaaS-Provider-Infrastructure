-- /etc/freeradius/version/clients.conf --
# NAS clients configuration for FreeRADIUS
client localhost {
        ipaddr = 127.0.0.1
        proto = *
        secret = <secret>
        nas_type = other

        limit {
                max_connections = 16
                lifetime = 0
                idle_timeout = 30
        }
}
