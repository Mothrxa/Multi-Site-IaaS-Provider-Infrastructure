-- /etc/dovecot/conf.d/10-mail.conf --
# Mailbox configuration for Dovecot IMAP service
mail_location = maildir:/var/mail/vmail/%d/%n/Maildir

namespace inbox {
  inbox = yes
}

mail_privileged_group = mail

protocol !indexer-worker {}