# /etc/nginx/sites-enabled/landing.conf
# STRATA Cloud — marketing landing page, port 80
server {
    listen 80;
    server_name _;

    root /var/www/landing;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
